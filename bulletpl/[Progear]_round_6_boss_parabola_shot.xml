bulletML(horizontal,[
  eaction(top,[
    repeat(25,[
      action([
        fire(some(dirAbs(190+rand*30)),none,bulletRef(seed,[1])),
        wait(15-rank*5),
        fire(some(dirAbs(350-rand*30)),none,bulletRef(seed,[- 1])),
        wait(15-rank*5)
      ])
    ]),
    wait(60)
  ]),
  ebullet(seed,bullet(none,some(spdAbs(1)),[
    action([
      changeSpeed(spdAbs(0),60),
      wait(60),
      fire(none,none,bullet(none,none,[])),
      fire(some(dirAbs(270+30* $1+rand*50* $1)),none,bulletRef(downAccel,[$1])),
      repeat(3,[
        action([fire(some(dirSeq(0)),some(spdSeq(- 0.4)),bulletRef(downAccel,[$1]))])
      ]),
      vanish
    ])
  ])),
  ebullet(downAccel,bullet(none,some(spdAbs(2.5)),[action([accel(none,some(spdAbs(4* $1)),120)])]))
]).
