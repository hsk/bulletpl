bulletML(horizontal,[
  top:action([
    repeat(50,[action([fire(dirAbs(270),spdAbs(1),bulletRef(rocket,[])),wait(10)])]),
    wait(120)
  ]),
  rocket:bullet(dirAim(0),spdAbs(1),[
    action([
      repeat(9999,[
        action([
          fireRef(udBlt,[90]),
          wait(20- $rank*8),
          fireRef(udBlt,[- 90]),
          wait($rand*10+15- $rank*8)
        ])
      ])
    ])
  ]),
  udBlt:fire(dirRel($1-25+ $rand*50),spdAbs(1),bullet(dirAim(0),spdAbs(1),[]))
]).
