bulletML(none,[
  eaction(top,[
    fire(some(dirAbs(175)),some(spdAbs(1+rank*4)),bullet(none,none,[])),
    repeat(30,[
      action([
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        wait(1),
        actionRef(rights,[]),
        actionRef(rights,[]),
        actionRef(rights,[]),
        actionRef(rights,[]),
        actionRef(rights,[]),
        actionRef(rights,[]),
        actionRef(rights,[]),
        actionRef(rights,[]),
        wait(15-rank*10),
        fire(some(dirSeq(4)),some(spdAbs(1+rank*4)),bullet(none,none,[]))
      ])
    ])
  ]),
  eaction(tops,[
    fire(some(dirAbs(185)),some(spdAbs(1+rank*4)),bullet(none,none,[])),
    repeat(30,[
      action([
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        fireRef(allway,[]),
        wait(1),
        actionRef(lefts,[]),
        actionRef(lefts,[]),
        actionRef(lefts,[]),
        actionRef(lefts,[]),
        actionRef(lefts,[]),
        actionRef(lefts,[]),
        actionRef(lefts,[]),
        actionRef(lefts,[]),
        wait(15-rank*10),
        fire(some(dirSeq(- 4)),some(spdAbs(1+rank*4)),bullet(none,none,[]))
      ])
    ])
  ]),
  eaction(lefts,[
    fire(some(dirSeq(- 0.7)),some(spdAbs(1+rank*4)),bullet(none,none,[])),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    wait(1)
  ]),
  eaction(rights,[
    fire(some(dirSeq(0.7)),some(spdAbs(1+rank*4)),bullet(none,none,[])),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    fireRef(allway,[]),
    wait(1)
  ]),
  efire(allway,fire(some(dirSeq(45)),some(spdAbs(1+rank*4)),bullet(none,none,[])))
]).
