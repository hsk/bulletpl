bulletML(vertical,[
  ebullet(stopAndRun,bullet(none,none,[
    action([
      changeSpeed(spdAbs(0),60),
      wait(180-rank*120),
      accel(none,some(spdAbs(3)),120),
      wait(120)
    ])
  ])),
  eaction(top,[
    repeat(200,[
      action([
        fire(some(dirAim(rand*360)),some(spdAbs(3*rand+0.5)),bulletRef(stopAndRun,[]))
      ])
    ]),
    wait(260-rank*120)
  ])
]).
