bulletML(vertical,[
  eaction(top,[
    repeat(50,[
      action([
        fire(some(dirSeq(30+1.1*5)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[])),
        repeat(11,[
          action([fire(some(dirSeq(30)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[]))])
        ]),
        repeat(4+rank*10,[
          action([
            wait(1),
            fire(some(dirSeq(31.1)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[])),
            repeat(11,[
              action([
                fire(some(dirSeq(30)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[]))
              ])
            ])
          ])
        ]),
        wait(10)
      ])
    ])
  ]),
  eaction(top2,[
    repeat(50,[
      action([
        fire(some(dirSeq(- 30-0.7*5)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[])),
        repeat(11,[
          action([
            fire(some(dirSeq(- 30)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[]))
          ])
        ]),
        repeat(4+rank*10,[
          action([
            wait(1),
            fire(some(dirSeq(- 30.7)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[])),
            repeat(11,[
              action([
                fire(some(dirSeq(- 30)),some(spdAbs(2+rank*rank*2)),bullet(none,none,[]))
              ])
            ])
          ])
        ]),
        wait(10)
      ])
    ])
  ])
]).
