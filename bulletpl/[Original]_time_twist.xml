bulletML(vertical,[
  eaction(top,[
    actionRef(ancient,[]),
    wait(60-40*rank),
    actionRef(future,[]),
    wait(60-40*rank),
    actionRef(modern,[]),
    wait(60-40*rank),
    actionRef(medieval,[]),
    wait(60-40*rank),
    actionRef(primal,[]),
    wait(450)
  ]),
  eaction(ancient,[
    fire(some(dirAbs(90)),some(spdAbs(1.5)),bulletRef(ancientBit,[])),
    fire(some(dirAbs(- 90)),some(spdAbs(1.5)),bulletRef(ancientBit,[])),
    fire(some(dirAbs(0)),some(spdAbs(0.0)),bulletRef(ancientBit,[]))
  ]),
  ebullet(ancientBit,bullet(none,none,[
    action([
      wait(30),
      changeSpeed(spdAbs(0),1),
      wait(5),
      repeat(40,[
        action([fire(some(dirAbs(180)),some(spdAbs(2.0)),bullet(none,none,[])),wait(5)])
      ]),
      vanish
    ])
  ])),
  eaction(modern,[
    repeat(20,[
      action([fire(some(dirAbs(- 45+90*rand)),none,bulletRef(modernBit,[])),wait(6)])
    ])
  ]),
  ebullet(modernBit,bullet(none,some(spdAbs(0.5)),[
    action([
      changeDirection(dirAbs(160+40*rand),90),
      wait(30),
      repeat(5+5*rank,[
        action([fire(some(dirAbs(- 45+90*rand)),none,bulletRef(modernScore,[])),wait(3)])
      ]),
      vanish
    ])
  ])),
  ebullet(modernScore,bullet(none,some(spdAbs(0.5)),[
    action([
      changeDirection(dirAbs(160+40*rand),90),
      changeSpeed(spdAbs(1.0+2.0*rank),300)
    ])
  ])),
  eaction(future,[fire(none,none,bulletRef(futureBit,[]))]),
  ebullet(futureBit,bullet(some(dirAbs(180)),some(spdAbs(1.5)),[
    action([
      wait(30),
      fire(some(dirAbs(360*rand)),none,bulletRef(futureTriangle,[])),
      repeat(10,[action([fire(some(dirSeq(120)),none,bulletRef(futureTriangle,[]))])]),
      vanish
    ])
  ])),
  ebullet(futureTriangle,bullet(none,some(spdAbs(1.1)),[
    action([
      wait(30),
      changeDirection(dirRel(120),1),
      wait(5),
      changeDirection(dirRel(150),60),
      repeat(20,[
        action([
          fire(some(dirRel(45)),some(spdAbs(0.8)),bullet(none,none,[])),
          wait(1),
          fire(some(dirRel(- 45)),some(spdAbs(1.0)),bullet(none,none,[])),
          wait(1),
          fire(some(dirRel(- 60)),some(spdAbs(1.2)),bullet(none,none,[])),
          wait(1)
        ])
      ])
    ])
  ])),
  eaction(medieval,[fire(none,none,bulletRef(medievalBit,[]))]),
  ebullet(medievalBit,bullet(some(dirAbs(180)),some(spdAbs(1.5)),[
    action([
      wait(60),
      changeSpeed(spdAbs(0.0),1),
      wait(5),
      repeat(50,[
        action([
          fire(some(dirAbs(360*rand)),none,bulletRef(medievalStar,[90])),
          fire(some(dirAbs(360*rand)),none,bulletRef(medievalStar,[- 90])),
          wait(2)
        ])
      ]),
      vanish
    ])
  ])),
  ebullet(medievalStar,bullet(none,some(spdAbs(1.1)),[
    action([
      wait(60),
      fire(some(dirAbs($1)),some(spdAbs(1.1)),bullet(none,none,[])),
      vanish
    ])
  ])),
  eaction(primal,[fire(none,none,bulletRef(primalBit,[]))]),
  ebullet(primalBit,bullet(some(dirAim(0)),some(spdAbs(0.5)),[
    action([
      changeSpeed(spdAbs(1.1),120),
      repeat(24,[action([changeDirection(dirAim(0),5),wait(5)])]),
      changeSpeed(spdAbs(0.0),120),
      repeat(24,[action([changeDirection(dirAim(0),5),wait(5)])]),
      wait(60),
      repeat(80+220*rank,[
        action([actionRef(primalRock,[180*rand,1]),actionRef(primalRock,[180*rand,- 1])])
      ]),
      vanish
    ])
  ])),
  eaction(primalRock,[
    fire(some(dirAbs($1* $2)),some(spdAbs((0.8+1.1* $1*(180- $1)div(90*90))*(0.5+0.5*rand)*(0.5+0.5*rank))),bullet(none,none,[]))
  ])
]).
