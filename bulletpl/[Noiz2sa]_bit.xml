bulletML(vertical,[
  top:action([repeat(4+ $rank*10,[action([fire(none,none,bulletRef(bit,[]))])]),wait(180)]),
  bit:bullet(none,spdAbs(0),[
    action([
      repeat(4,[
        action([
          changeDirection(dirAbs($rand*360),20),
          changeSpeed(spdAbs(2),20),
          wait(20),
          changeSpeed(spdAbs(0),20),
          wait(20),
          fire(none,none,bulletRef(seed,[])),
          wait(0)
        ])
      ]),
      vanish
    ])
  ]),
  seed:bullet(none,spdAbs(0),[
    action([
      fire(dirAim($rand*10-5),none,bulletRef(nrm,[])),
      repeat(5,[action([wait(6),fire(dirSeq(0),none,bulletRef(nrm,[]))])]),
      vanish
    ])
  ]),
  nrm:bullet(none,spdAbs(2),[])
]).
