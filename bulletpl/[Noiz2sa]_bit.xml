bulletML(vertical,[
  top:action([
    repeat(4+ $rank*10,[action([fire(dirAim(0),spdAbs(1),bulletRef(bit,[]))])]),
    wait(180)
  ]),
  bit:bullet(dirAim(0),spdAbs(0),[
    action([
      repeat(4,[
        action([
          changeDirection(dirAbs($rand*360),20),
          changeSpeed(spdAbs(2),20),
          wait(20),
          changeSpeed(spdAbs(0),20),
          wait(20),
          fire(dirAim(0),spdAbs(1),bulletRef(seed,[])),
          wait(0)
        ])
      ]),
      vanish
    ])
  ]),
  seed:bullet(dirAim(0),spdAbs(0),[
    action([
      fire(dirAim($rand*10-5),spdAbs(1),bulletRef(nrm,[])),
      repeat(5,[action([wait(6),fire(dirSeq(0),spdAbs(1),bulletRef(nrm,[]))])]),
      vanish
    ])
  ]),
  nrm:bullet(dirAim(0),spdAbs(2),[])
]).
