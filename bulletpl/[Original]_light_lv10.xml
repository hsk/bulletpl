bulletML(vertical,[
  eaction(top,[
    repeat(10,[
      action([
        fire(some(dirAbs(180+18)),some(spdAbs(3)),bulletRef(bit,[1.7,30,- 144])),
        wait(15),
        fire(some(dirAbs(180-18)),some(spdAbs(3)),bulletRef(bit,[1.7,30,144])),
        wait(30)
      ])
    ]),
    wait(450)
  ]),
  ebullet(bit,bullet(none,none,[
    action([
      wait($2),
      changeSpeed(spdAbs(0),1),
      fire(some(dirRel($3)),some(spdAbs($1)),bulletRef(bit,[$1,$2*1.2,$3])),
      fire(some(dirSeq(0)),some(spdAbs(0)),bulletRef(nobit,[$1*(0.5+0.5*rank)])),
      repeat(15,[
        action([fire(some(dirSeq(0)),some(spdAbs($1)),bulletRef(weak,[$2*1.2])),wait(2)])
      ]),
      vanish
    ])
  ])),
  ebullet(nobit,bullet(none,none,[
    action([
      fire(some(dirAim(0)),some(spdAbs($1)),bullet(none,none,[])),
      repeat(4,[
        action([fire(some(dirSeq(0)),some(spdAbs($1)),bullet(none,none,[])),wait(2)])
      ]),
      vanish
    ])
  ])),
  ebullet(weak,bullet(none,none,[action([wait($1),vanish])]))
]).
