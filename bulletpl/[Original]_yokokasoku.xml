bulletML(none,[
  accelShot:fire(dirAbs(90),spdAbs(0),bullet(dirAim(0),spdAbs(1),[action([accel(spdAbs(3* $1),spdRel(0),80)])])),
  top:action([
    repeat(20,[
      action([
        fire(dirAim($rand*90-45),spdAbs(1),bullet(dirAim(0),spdAbs(1),[
          action([
            repeat(9999,[
              action([
                wait($rand*20+30- $rank*20),
                fireRef(accelShot,[1]),
                fireRef(accelShot,[- 1])
              ])
            ])
          ])
        ])),
        wait(10)
      ])
    ]),
    wait(120)
  ])
]).
