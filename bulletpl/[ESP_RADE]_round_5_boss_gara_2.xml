bulletML(none,[
  featherShot:bullet(dirAim(0),spdAbs(6),[
    action([
      changeSpeed(spdAbs(0),20),
      wait(20),
      fire(dirAim(0),spdAbs(1),bulletRef(featherAim,[])),
      repeat(150+ $rank*100,[
        action([
          fire(dirAim(90* $rand-45),spdAbs(1),bullet(dirAim(0),spdAbs(1),[])),
          wait(3- $rank*2)
        ])
      ]),
      vanish
    ])
  ]),
  featherAim:bullet(dirAim(0),spdAbs(0),[
    action([
      repeat(7,[
        action([
          fire(dirAim(0),spdAbs(3),bullet(dirAim(0),spdAbs(1),[])),
          repeat(20,[
            action([wait(2),fire(dirSeq(0),spdAbs(3),bullet(dirAim(0),spdAbs(1),[]))])
          ]),
          wait(30)
        ])
      ]),
      vanish
    ])
  ]),
  top:action([
    fire(dirAbs(90),spdAbs(1),bulletRef(featherShot,[])),
    fire(dirAbs(- 90),spdAbs(1),bulletRef(featherShot,[])),
    wait(550)
  ])
]).
