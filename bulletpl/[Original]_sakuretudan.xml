bulletML(none,[
  top:action([
    repeat(10,[
      action([
        fire(dirAim($rand*360),spdAbs(2),bullet(none,none,[
          action([
            changeDirection(dirAim(0),60),
            wait(60),
            repeat(15+ $rank*20,[
              action([
                repeat(2,[
                  action([
                    fire(dirAim(360* $rand),spdAbs(5),bullet(none,none,[
                      action([
                        wait($rand*5),
                        fire(none,spdAbs($rand*4+0.5),bullet(none,none,[])),
                        vanish
                      ])
                    ]))
                  ])
                ]),
                wait(1)
              ])
            ]),
            vanish
          ])
        ])),
        wait(60)
      ])
    ]),
    wait(100)
  ])
]).
