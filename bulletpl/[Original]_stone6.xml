bulletML(vertical,[
  eaction(top,[
    fire(some(dirAbs(150)),some(spdAbs(4)),bulletRef(roll,[])),
    wait(2),
    fire(some(dirAbs(210)),some(spdAbs(4)),bulletRef(roll,[])),
    wait(2),
    fire(some(dirAbs(135)),some(spdAbs(3)),bulletRef(roll,[])),
    wait(2),
    fire(some(dirAbs(225)),some(spdAbs(3)),bulletRef(roll,[])),
    wait(2),
    fire(some(dirAbs(135)),some(spdAbs(2)),bulletRef(roll,[])),
    wait(2),
    fire(some(dirAbs(225)),some(spdAbs(2)),bulletRef(roll,[])),
    wait(60)
  ]),
  ebullet(roll,bullet(none,none,[
    action([
      wait(10),
      changeSpeed(spdAbs(0),1),
      repeat(45,[
        action([fire(some(dirSeq(8)),some(spdAbs(1.3+rank)),bullet(none,none,[]))])
      ]),
      vanish
    ])
  ]))
]).
