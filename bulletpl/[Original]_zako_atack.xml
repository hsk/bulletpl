bulletML(none,[
  eaction(top,[
    repeat(40+rank*20,[
      action([
        wait(4),
        fire(some(dirAim(rand*180-90)),some(spdAbs(1.5)),bullet(none,none,[
          action([
            repeat(3,[
              action([
                fire(some(dirAim((0.5-rand)*rank*10)),some(spdAbs(1.5)),bullet(none,none,[])),
                wait(20+rand*rank*40)
              ])
            ])
          ])
        ]))
      ])
    ])
  ])
]).
